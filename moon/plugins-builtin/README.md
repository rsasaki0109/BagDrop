# Built-in Analyzer Plugins

Planned statically linked analyzers:

- `builtin.generic`
- `builtin.trajectory`
- `builtin.gnss-basic`

Dynamic `.bdp` plugins are deferred until the first-party ABI and result model stabilize.
